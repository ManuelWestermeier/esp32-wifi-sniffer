[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

; Serial monitor speed
monitor_speed = 115200

; NimBLE-Arduino for BLE NUS (PlatformIO registry)
lib_deps =
  h2zero/NimBLE-Arduino@^2.1.0

; If you prefer a newer pinned version you can change the @^2.1.0 part.
